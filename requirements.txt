# ---------------------------------------------------------
# 1. Web Framework, Async Server & Templating
# ---------------------------------------------------------
fastapi==0.111.0
uvicorn[standard]==0.30.1
jinja2==3.1.4
python-multipart==0.0.9

# ---------------------------------------------------------
# 2. Database & Persistence Layer (PostgreSQL)
# ---------------------------------------------------------
sqlalchemy==2.0.31
alembic==1.13.1
asyncpg==0.29.0          # Async Postgres driver for non-blocking DB calls
psycopg2-binary==2.9.9   # Sync fallback driver for Alembic migrations

# ---------------------------------------------------------
# 3. Vector Database (Local RAG & Dual-Write)
# ---------------------------------------------------------
lancedb==0.6.13          # Serverless, fast, Arrow-based vector DB
chromadb==1.5.9          # Vector DB for Fallback

# ---------------------------------------------------------
# 4. LLM Client & Agent Framework (Mesh API Compliant)
# ---------------------------------------------------------
openai==1.35.3
httpx==0.27.2
langgraph==0.1.5
langchain-core==0.2.10

# ---------------------------------------------------------
# 5. Background Tasks & Scheduling
# ---------------------------------------------------------
apscheduler==3.10.4
celery==5.3.4
redis==5.1.0

# ---------------------------------------------------------
# 6. Observability, Configuration & Utilities
# ---------------------------------------------------------
langsmith==0.1.82
langchain-openai==0.2.10
python-dotenv==1.0.1
pydantic==2.7.4
pydantic-settings==2.3.4

# ---------------------------------------------------------
# 7. Data Processing & Statistical Analytics
# ---------------------------------------------------------
polars==1.3.0            # Core data manipulation for sweeps
numpy==1.26.4            # Array operations for Jacobian matrices
scipy==1.13.1            # Chi-Squared statistical testing
networkx==3.3            # DAG Graph construction and topological sorting